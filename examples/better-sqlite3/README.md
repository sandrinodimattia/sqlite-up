# better-sqlite3 Example

This example uses `better-sqlite3`.

Build the library from the repository root first:

```bash
pnpm run build
```

Then install and run this example:

```bash
cd examples/better-sqlite3
pnpm install
pnpm start
```
