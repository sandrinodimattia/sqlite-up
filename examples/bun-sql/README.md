# Bun SQL Example

This example uses Bun's Promise-based `SQL` SQLite client.

Build the library from the repository root first:

```bash
pnpm run build
```

Then install and run this example:

```bash
cd examples/bun-sql
bun install
bun start
```
