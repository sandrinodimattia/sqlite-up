# Bun SQLite Example

This example uses Bun's synchronous `bun:sqlite` database.

Build the library from the repository root first:

```bash
pnpm run build
```

Then install and run this example:

```bash
cd examples/bun-sqlite
bun install
bun start
```
