# node:sqlite Example

This example uses Node 24+ `node:sqlite`.

Build the library from the repository root first:

```bash
pnpm run build
```

Then install and run this example:

```bash
cd examples/node-sqlite
pnpm install
pnpm start
```
